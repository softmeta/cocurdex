---
name: cocurdex-spec
description: Draft a technical spec and save it as an app-owned Cocurdex note.
---

# Cocurdex Spec

Write the spec in Markdown and persist it with `cocurdex note create`. Update
the returned stable id with `cocurdex note update <id>`. Do not write directly
to SQLite or a `.cocurdex` file tree.
